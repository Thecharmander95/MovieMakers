%h1 Credit for #{@movie.name}
- @movie.credits.each do |credit|
  %h5= credit.person
  %br/
  %h5= credit.for
%br/
.hide_on_print
  = link_to "Print this Page", "javascript:print()", class: "button"
  %br/
  = link_to "Back", movie_path(@movie), class:"button"
